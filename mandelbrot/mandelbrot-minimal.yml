# Minimal Mandelbrot Configuration
# Uses default size (1024x768) and default max_iterations (128)

fractal:
  type: Mandelbrot

color_scheme:
  type: WhiteOnBlack

image:
  upperLeft: -2.5+1.5i
  lowerRight: 1.5+-1.5i
